server:
  port: 9001
spring:
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: public # ??????????????# ??????????????
        file-extension: yml # ??????????properties
        # group: DEFAULT_GROUP # ??????DEFAULT_GROUP
        refresh-enabled: true #??????
        prefix: service_consumer # 默认值为spring.applicaion.name 可以指定
  application:
    name: nacos_config

